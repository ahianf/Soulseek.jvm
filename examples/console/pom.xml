<?xml version="1.0" encoding="UTF-8"?>
<!-- SPDX-FileCopyrightText: JP Dillingham -->
<!-- SPDX-FileCopyrightText: 2026 Ahian Fernandez -->
<!-- SPDX-License-Identifier: GPL-3.0-only -->

<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>tenine.example</groupId>
  <artifactId>slsk-jvm-console-example</artifactId>
  <version>0.10.0.2</version>
  <name>Soulseek.jvm console example</name>
  <description>
    Minimal external-consumer example for the modified Soulseek.jvm public
    API, based on Soulseek.NET 10.0.2.
  </description>
  <url>https://github.com/jpdillingham/Soulseek.NET/tree/bc9f7ca</url>

  <licenses>
    <license>
      <name>GNU General Public License v3.0 only with Additional Terms</name>
      <url>https://github.com/jpdillingham/Soulseek.NET/blob/bc9f7ca/LICENSE</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.build.outputTimestamp>2026-07-05T02:28:40Z</project.build.outputTimestamp>
    <maven.compiler.release>21</maven.compiler.release>
    <maven.compiler.version>3.15.0</maven.compiler.version>
    <maven.enforcer.version>3.5.0</maven.enforcer.version>
    <maven.jar.version>3.5.0</maven.jar.version>
    <maven.resources.version>3.4.0</maven.resources.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>dev.slsk</groupId>
      <artifactId>slsk-jvm</artifactId>
      <version>0.10.0.2</version>
    </dependency>
  </dependencies>

  <build>
    <resources>
      <resource>
        <directory>${project.basedir}/../../..</directory>
        <includes>
          <include>LICENSE</include>
          <include>NOTICE</include>
        </includes>
      </resource>
      <resource>
        <directory>${project.basedir}/../../..</directory>
        <targetPath>META-INF</targetPath>
        <includes>
          <include>LICENSE</include>
          <include>NOTICE</include>
        </includes>
      </resource>
    </resources>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>
        <version>${maven.resources.version}</version>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>${maven.enforcer.version}</version>
        <executions>
          <execution>
            <id>enforce-build-environment</id>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <requireJavaVersion>
                  <version>[21,)</version>
                </requireJavaVersion>
                <requireMavenVersion>
                  <version>[3.9,)</version>
                </requireMavenVersion>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${maven.compiler.version}</version>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>${maven.jar.version}</version>
        <configuration>
          <archive>
            <manifestEntries>
              <Implementation-Title>Soulseek.jvm console example</Implementation-Title>
              <Implementation-Version>${project.version}</Implementation-Version>
              <Implementation-Vendor>Ahian Fernandez</Implementation-Vendor>
              <Bundle-License>GPL-3.0-only</Bundle-License>
              <X-Modified-From>Soulseek.NET 10.0.2 (bc9f7ca)</X-Modified-From>
            </manifestEntries>
          </archive>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
